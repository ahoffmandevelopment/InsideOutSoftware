{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy to SharkASP",
            "type": "shell",
            "command": "dotnet publish -c Release && echo 'Build completed, now upload files using SFTP extension'",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}